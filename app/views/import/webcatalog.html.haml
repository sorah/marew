%h1 Import webcatalog (manual)

= form_tag webcatalog_import_submit_path, method: :post do
  %p
    = select_tag :event_id, options_for_select(@events)
  %p
    = text_area_tag :json
  %p
    = submit_tag 'Go!'
